# cargo-fuzz build artifacts and runtime outputs
target
corpus
artifacts
coverage

# Regenerated by `cargo fuzz build`; this crate is unpublished dev tooling and its
# dependencies are already pinned by the tracked root Cargo.lock.
Cargo.lock
